# 本地 AI 图片与视频生成部署计划

更新日期：2026-09-16（北京时间）

## 目标与当前状态

在本机部署 ComfyUI，先用 SDXL 跑通图片生成，再用 Wan 2.2 TI2V-5B 跑通文生视频和图生视频。先验证基础工作流，再考虑增加模型、插件和加速配置。

当前阶段：基础部署与生成验证已完成。ComfyUI v0.36.0、SDXL、Wan 2.2 TI2V-5B 均已安装；两次图片生成、一次文生视频、一次图生视频成功。服务保留运行，地址为 http://127.0.0.1:8188 。使用说明见 README.md。

项目目录：`C:\Users\76797\Desktop\chatgpt\aivideo`

## 已确认的硬件

| 项目 | 实测结果 | 影响 |
| --- | --- | --- |
| 电脑 | Dell XPS 8960 | Windows 本地部署 |
| 显卡 | NVIDIA GeForce RTX 4090 D | 支持 CUDA 推理 |
| 总显存 | 24564 MiB，约 24GB | 适合 SDXL；达到 Wan 2.2 5B 官方低显存方案的显存档位 |
| 检查时空闲显存 | 22141 MiB，约 21.6GiB | 会随其他程序变化，视频测试前需重新检查 |
| 驱动版本 | 610.60 | 安装时与选定 PyTorch 版本核对兼容性 |
| 系统内存 | 68352516096 字节，约 64GB | 可供模型卸载使用；实际峰值待测 |
| C 盘可用空间 | 约 445GiB | 当前项目盘；下载前重新检查 |
| D / E 盘可用空间 | 约 464 / 515GiB | 可作为后续模型存储候选位置 |

CPU 型号尚未可靠记录，不作为当前判断依据。上述磁盘为逻辑盘，尚未确认介质类型。硬件满足初步尝试条件，速度、稳定性和最终质量必须通过实际生成确认。

## 选型依据与边界

- ComfyUI：作为图片和视频工作流的统一操作界面，保存可重复执行的工作流。
- SDXL：先建立图片生成基线，减少首次部署同时引入的变量。具体权重、来源和许可证在下载前记录。
- Wan 2.2 TI2V-5B：同时支持文字和图片输入。官方单卡示例在至少 24GB 显存上运行，使用模型卸载、权重类型转换和 CPU 文本编码器配置；官方示例分辨率为 1280×704 或 704×1280。
- 官方脚本的显存条件不能直接视为 ComfyUI 工作流的保证。接入 ComfyUI 时需核对实际模型格式、文本编码器、VAE 和内存管理方式。
- Wan 2.2 A14B 不纳入第一阶段：官方部分单卡示例要求 80GB 显存。后续如需尝试，另行评估量化和卸载方案。
- 不预先承诺每张图或每段视频的耗时。最终模型版本的使用条件按各自许可证确认。

参考来源：

- [ComfyUI 官方仓库](https://github.com/Comfy-Org/ComfyUI)
- [ComfyUI 系统要求](https://docs.comfy.org/installation/system_requirements)
- [Wan 2.2 官方仓库与运行示例](https://github.com/Wan-Video/Wan2.2)
- [ComfyUI 的 Wan 2.2 官方教程](https://docs.comfy.org/tutorials/video/wan/wan2_2)

## 分阶段执行步骤

### 1. 环境检查与安装方式确认

1. 检查 Windows 版本、现有 Python/Git/ComfyUI、GPU 驱动和端口占用，避免覆盖现有环境。
2. 核对当时的 ComfyUI 官方 Windows 安装说明。优先评估 NVIDIA 便携包，使用独立运行环境；若不满足模型支持要求，再选择独立 Python 虚拟环境，并记录原因。
3. 确认模型预计下载体积、安装盘和剩余空间。暂以当前项目目录为部署根目录；大模型迁移到其他盘时再记录实际位置。
4. 记录安装包来源、ComfyUI 版本或提交号、Python/PyTorch/CUDA 运行时版本。

完成标准：安装方式与实际目录明确，依赖与驱动兼容，未破坏现有环境。

### 2. 安装并启动 ComfyUI

1. 从官方来源获取选定安装包或代码，建立独立环境。
2. 先启动基础 ComfyUI，不批量安装第三方节点。
3. 验证 CUDA 可用、显卡识别正确、界面可访问、启动日志无阻断错误。
4. 默认仅监听本机地址，保存启动命令与日志；退出时确认进程正确结束。

完成标准：可以重复启动和停止，并明确界面地址、启动入口与日志位置。

### 3. SDXL 图片生成基线

1. 确认 SDXL 权重来源、版本、许可证及所需组件，记录实际文件名和目录。
2. 使用基础文生图工作流，先以单张、1024×1024、固定种子测试；若所选模型有不同推荐参数，以模型说明为准并记录。
3. 保存提示词、采样参数、工作流、图片、耗时及可采集的资源数据。
4. 重复运行一次确认稳定性，验证结果可正常打开且不是黑图或明显损坏。

完成标准：至少一张正常图片及可再次运行的工作流，能够从记录复现生成配置。

### 4. Wan 2.2 TI2V-5B 视频生成基线

1. 优先使用当前 ComfyUI 官方教程和模板，核对扩散模型、文本编码器、VAE 的具体格式与对应目录。
2. 配置适用于 24GB 显存的内存管理方式；不要直接照搬官方 Python 脚本参数到 ComfyUI。
3. 先按模板支持的较短帧数生成短视频。分辨率遵循模型要求，资源不足时再逐项降低；记录实际帧数、帧率和时长。
4. 分别验证文生视频与图生视频，图生视频可使用上一阶段生成的图片。
5. 保存输入、输出、工作流、种子、采样参数、耗时和错误日志，检查视频可解码、可播放、帧数正确且内容非空白。

完成标准：至少各一段可播放的文生视频、图生视频，以及对应可重复执行的工作流。记录实际可行参数，不把一次成功视为所有分辨率和时长均可运行。

### 5. 交付与可选优化

1. 整理启动/停止方法、模型清单、图片与视频工作流、输出目录及常见故障处理。
2. 根据实测瓶颈再评估量化、加速模型、额外插件、超分辨率或更长视频。
3. 每次优化仅改变少量变量，与基线比较速度、资源占用和质量；保留已成功的工作流。

完成标准：用户可根据文档独立启动并运行图片、视频工作流，所有已知限制已记录。

## 计划目录结构

实际使用官方便携包目录结构，避免移动内置 Python 及更新脚本造成路径问题：

```text
aivideo/
  DEPLOYMENT.md             部署计划、状态与试错记录
  ComfyUI_windows_portable/
    ComfyUI/               主程序，models/ 保存模型，input/ 保存输入
    python_embeded/        独立 Python 与依赖
    update/                官方更新脚本
  downloads/               安装包、模型元数据
  workflows/               已验证的图片和视频工作流
  logs/                    启动日志、错误日志、测试数据
  outputs/                 已通过启动参数设为实际生成输出目录
  start-comfyui.bat         双击启动（后台运行，日志保存到 logs）
  stop-comfyui.bat          双击停止本项目记录的服务进程
  start-comfyui.ps1         启动实现
  stop-comfyui.ps1          停止实现
```

模型按所选工作流要求存放在 ComfyUI 的对应模型目录。若实际安装结构或输出路径不同，及时替换本节，避免文档与实际不一致。

## 排错与更新规则

后续每次发生安装方式、模型版本、路径、参数或依赖变更，执行后立即更新本文相关段落，并追加下面的记录。失败也记录，不只保留最终成功结果。不得将尚未验证的解决方案标为成功。

每条记录至少包括：时间、所在阶段、症状、关键错误或日志路径、原因判断（区分已证实和推测）、采取的修改、复测结果及下一步。

推荐排错顺序：

1. 显存不足：先检查其他程序占用，再调整批量、分辨率、帧数或卸载配置；必要时评估量化。
2. 内存不足：检查进程占用与卸载造成的内存峰值，再减少任务规模。
3. 模型或节点不匹配：核对官方工作流、文件名、目录、模型格式及 ComfyUI 版本。
4. 下载失败：记录来源、错误、未完成文件，优先续传并核对官方提供的大小或校验信息。
5. 依赖冲突：在独立环境中调整，记录变更前后版本；保留恢复到已验证配置的方法。

## 执行与试错记录

| 日期 | 阶段 | 现象 / 动作 | 结果与下一步 |
| --- | --- | --- | --- |
| 2026-09-16 | 硬件检查 | nvidia-smi 读取 GPU 与显存；读取磁盘空间 | 已确认 RTX 4090 D 24GB，磁盘空间足够启动部署 |
| 2026-09-16 | 硬件检查 | 沙箱内 Get-CimInstance 返回拒绝访问；授权后重新查询 | 已读取约 64GB 内存；CPU 型号未可靠记录 |
| 2026-09-16 | 规划 | 编写部署阶段、验证标准和更新规则 | 仅完成文档；下一步为检查环境并确认 ComfyUI 安装方式 |

## 进度清单

- [x] 硬件初步检查
- [x] 部署计划写入项目目录
- [x] 环境检查、确认安装方式和具体版本
- [x] 安装并验证 ComfyUI
- [x] 下载 SDXL 并验证图片工作流
- [x] 下载 Wan 2.2 TI2V-5B 所需组件
- [x] 验证文生视频
- [x] 验证图生视频
- [x] 整理启动入口、成果与实测结果

### 安装执行记录（2026-09-16，按执行顺序保留）

- 已创建 downloads、workflows、logs、outputs 目录。
- 安装方式：官方 NVIDIA 便携包 v0.36.0，约 1.9GB；保留内置 Python 与独立依赖。
- 网络访问在沙箱中被拒绝，授权联网后已读取官方发布信息。
- 首次 curl 下载失败：CRYPT_E_REVOCATION_OFFLINE，证书吊销服务不可达；重试采用 --ssl-revoke-best-effort，仍验证 TLS 证书。
- 官方安装包 SHA256：c3c60192840f8b68c9a47cf3e8161ecb108e5ffdf5ea236c1c72a402e442695d；下载完成后核验。
- 安装包已通过上述 SHA256 校验，使用 Windows 自带 tar 解压成功，无需额外安装 7-Zip。
- 实测环境：Python 3.13.14、PyTorch 2.13.0+cu130、CUDA 13.0、ComfyUI 0.36.0、前端 1.52.7、模板 0.11.62。CUDA 张量运算成功。
- 服务地址：http://127.0.0.1:8188 ，仅监听本机，关闭云端 API 节点。系统状态快照：logs/system-stats.json。
- 启动首次初始化约需数十秒，过早访问时出现连接拒绝；初始化完成后系统状态接口和首页正常，不属于启动失败。
- Hugging Face 直连两次连接超时，改用 HF Mirror 下载同一仓库的固定提交；下载后按仓库 LFS SHA256 校验。镜像是第三方传输通道，元数据快照保留在 downloads 下。
- SDXL 固定提交：462165984030d82259a11f4367a4eed129e94a7b；Wan 重打包仓库固定提交：c4f60d30c55a624e35427060fdd217579a6c1d77。
- 模型总下载体积约 25.1GB（十进制）：SDXL 6.94GB、Wan 5B 10.00GB、文本编码器 6.74GB、VAE 1.41GB。下载中使用 .part 后缀，校验成功后再启用。

- 四个模型已全部完成下载与 SHA256 校验，校验结果见 logs/model-verification.json。
- 项目启动、停止、重新启动已验证；官方内置模板衍生出的三套工作流已放入 workflows 与 ComfyUI 用户工作流目录。
- 已开始 SDXL 1024×1024、25 步的首次生成测试。GPU 占用以约 3 秒间隔采样，统计包含其他程序占用，不是精确单任务峰值。

- SDXL 两次生成成功：1024×1024、25 步、种子 42。脚本端耗时分别 9.69 秒和 9.32 秒（含轮询等待）；第二次先卸载模型以避免直接命中缓存。图片已目视检查，无黑图或损坏。输出在 outputs/sdxl。
- 已将首张图片复制为 ComfyUI/input/sdxl-reference.png，供后续图生视频测试。正在测试 Wan 1280×704、49 帧、24fps、20 步。

- Wan 文生视频成功：1280×704、49 帧、24fps、20 步，脚本端耗时 70.31 秒；输出 outputs/wan/t2v_baseline_00001_.mp4。开始相同参数的图生视频验证。

## Blender + 深度图控制视频（2026-09-17 起）

目标：Blender 出精确几何与相机运动，ComfyUI 用 Wan2.2 Fun Control 5B 按深度序列生成画面，解决"围栏、剪角"这类形状必须准确的镜头。角色一致性不由这条路线负责。

- Blender 4.2.23 LTS 便携版：官方 download.blender.org 下载，SHA256 `82e79147…d910cb` 与官方 blender-4.2.23.sha256 一致，解压到 `tools/blender/blender-4.2.23-windows-x64/`，不写注册表。无头运行验证通过，Cycles 可见 RTX 4090 D（OptiX）与 i9-14900K。
- 场景脚本 `tools/blender/render-depth-sequence.py`：程序化搭建 12×8 围栏（每 2 单位一根柱、两道横杆），右上角按 `--cut` 斜切，相机绕剪角处 70° 环绕并缓慢下降；合成器把 Z 通道按 8–28 单位映射为近白远黑的 8 位灰度。首次渲染 49 帧 1280×704 用时 26 秒（EEVEE，GPU）。输出 `productions/math/tests/depth/fence-v001/`（depth/、preview/、scene.blend、preview.mp4）。
- 控制视频：`ComfyUI/input/fence-v001-depth.mp4`（H.264 无损级 crf 10，24fps，49 帧）。
- 模型：`wan2.2_fun_control_5B_bf16.safetensors`（10.00 GB，Comfy-Org 重打包仓库同一提交 c4f60d30，SHA256 已在 downloads/wan-metadata.json），经 HF Mirror 分段续传，verify-models.py 已加入该文件。文本编码器与 VAE 复用现有 umt5 fp8 与 wan2.2_vae。
- 工作流 `04-wan-fun-control-depth`：由官方模板 video_wan2_2_5B_fun_control 派生，Canny 与参考图节点旁路，直接把深度视频送入 Wan22FunControlToVideo；1280×704、49 帧、20 步、cfg 5、种子 42。ComfyUI 核心自带该节点，未装任何第三方节点。
- **基线 v1 结果（09-17 17:08）**：权重 SHA256 校验通过；工作流一次成功，脚本端 67.4 秒，采样峰值整卡显存 21689 MiB，输出 `outputs/wan/fun_control_depth_baseline_00001_.mp4`（1280×704、49 帧、2.04 秒）。抽帧（productions/math/tests/depth/fence-v001/result/）：画风符合提示词（卡通木栅栏、草地、木箱），但几何跟随弱：围栏只剩前方一段与右侧一角，没有闭合的矩形和斜切角，相机 70° 环绕几乎没有体现。判断：深度图里细柱细杆占比太小、地面渐变主导；且 5B 控制力有限。下一步：改用预览渲染的 Canny 线稿做控制（结构线条更强）、收窄深度映射范围，两者各跑一次对比。
- **三种控制信号对比（09-17 17:14，同一提示词、种子 42、20 步、1280×704×49）**：

| 工作流 | 控制信号 | 脚本端耗时 | 几何跟随 | 输出 |
| --- | --- | --- | --- | --- |
| 04-wan-fun-control-depth | 深度图，映射 8–28 | 67.4 秒 | 弱：只剩前段围栏，无闭合矩形和斜切角，相机几乎不动 | outputs/wan/fun_control_depth_baseline_00001_.mp4 |
| 04c-wan-fun-control-depth-tight | 深度图，映射 10–22 | 64.3 秒 | 与上一条逐帧一致，说明深度灰度对该模型基本不起作用 | outputs/wan/fun_control_depth_tight_00001_.mp4 |
| **04b-wan-fun-control-canny** | Blender 平光预览 → ComfyUI Canny（0.1/0.4） | 67.3 秒 | **好：闭合围栏、每根柱子、斜切角、标记箱、70° 环绕全部对应** | outputs/wan/fun_control_canny_baseline_00001_.mp4 |

  结论：这条基线改为"Blender 平光渲染 → Canny 线稿 → Fun Control"，深度图路线在 Wan2.2 Fun Control 5B 上不可用（两种映射结果完全相同）。Blender 脚本保留深度输出但不再作为控制信号；后续可换 Blender Freestyle 线稿替代 Canny 以获得更干净的边缘。抽帧对照在 productions/math/tests/depth/fence-v001/result/。
- 选 5B 而非 14B 的原因：与已验证的 TI2V-5B 同架构、同编码器和 VAE，10 GB 即可跑通流程；官方模板注明 5B 质量一般，跑通后再评估 14B fp8（两个 13.3 GB 文件）。

## 最终验证结果（2026-09-16）

| 测试 | 参数 | 脚本端耗时 | 采样最高整卡显存占用 | 结果 |
| --- | --- | --- | --- | --- |
| SDXL 第一次 | 1024×1024，25 步，种子 42 | 9.69 秒 | 9906 MiB | 成功 |
| SDXL 重复生成 | 同上，先卸载模型；执行历史确认缓存节点列表为空 | 9.32 秒 | 9916 MiB | 成功 |
| Wan 文生视频 | 1280×704，49 帧，24fps，20 步，种子 42 | 70.31 秒 | 21585 MiB | 成功 |
| Wan 图生视频 | 同上，以首张 SDXL 图片作为输入 | 70.35 秒 | 23322 MiB | 成功 |

耗时包含请求与轮询等待；显存每约 3 秒采样一次，包含桌面和其他程序占用，不是精确瞬时峰值。视频均为 H.264 MP4、49 帧、2.0417 秒，无音轨。已完整解码所有帧，并检查抽取的中间帧；两段视频首尾画面存在变化，无全黑输出。验证报告见 `logs/media-validation.json`，对应执行记录为 `logs/01-*`、`logs/02-*`、`logs/03-*`。

成果：

- `outputs/sdxl/baseline_00001_.png`
- `outputs/sdxl/baseline_00002_.png`
- `outputs/wan/t2v_baseline_00001_.mp4`
- `outputs/wan/i2v_baseline_00001_.mp4`

模型文件：均已校验，准确大小、SHA256 和仓库提交见 `logs/model-verification.json`。便携包解压后的代码版本为 0.36.0，完整依赖快照见 `logs/python-packages.txt`。

界面验证：浏览器可打开中文版 ComfyUI，工作流侧栏能看到三套项目工作流；模型下载完成并刷新后，没有缺失模型提示，图生视频工作流可正常加载。不需要登录云端账号。

当前默认工作流适合作为短片基线。本次未验证更长视频、多任务并行、更高分辨率或其他模型；图生视频整卡显存采样接近 24GB 档位，增加帧数后需要重新实测。输入样例是正方形，图生视频按 1280×704 自动裁切，实际使用时可调整宽高比。

部署过程中解决的问题：沙箱联网限制、Windows 吊销检查离线、Hugging Face 直连超时、Python 控制台 GBK 无法输出模板 emoji（检查脚本改用 `-X utf8`）。未修改 ComfyUI 核心代码，未添加第三方自定义节点。

维护脚本：`prepare-workflows.py` 从随包官方模板生成本项目基线，重运行会覆盖同名项目工作流；`verify-models.py` 完成下载校验与启用；`run-workflow.py` 发起本地生成并记录结果；`inspect-results.py` 解码成果并保存检查数据。修改工作流前保留副本。
